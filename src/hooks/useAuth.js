// // hooks/auth.js

// import { useDispatch } from 'react-redux';
// import { login as loginAction, logout as logoutAction } from '../redux/reducers/UserInfoSlice';

// export const useAuth = () => {
//   const dispatch = useDispatch();

//   const login = (role) => {
//     dispatch(loginAction(role));
//   };

//   const logout = () => {
//     dispatch(logoutAction());
//   };

//   return { login, logout };
// };
